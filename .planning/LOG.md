# LOG.md — Richmond Regional Housing Framework 2026 Data Update

Session history. Append-only, newest first. A correction is a new entry at the top saying what it corrects — never an edit to the entry below.

Entries dated before 2026-08-05 are carried over verbatim from the retired PLAN.md Section 11 progress log. They keep their original `§` references and their original task numbering, both of which predate the `.planning/` migration. Read them as historical records, not as current pointers.

## Kickoff prompt for the next session

This block is overwritten by `hda-docs:wrap` each time a session closes — never appended to, and not itself an entry.

> Plan Phase 2 (demand data). This is a planning-only session: read CLAUDE.md, `.planning/PLAN.md` Sections 1 through 4, and the Section 5 rows for datasets 1 through 5 and 19. Produce `.planning/phases/02-demand-data.md` from the `hda-docs` phase-file skeleton, scoping `r/geo.R`, `r/acs_demographics.R`, `r/acs_income.R`, `r/pep.R`, `r/decennial.R`, and `r/wcoop.R` into tasks with checkable Verify lines. Write no R code and pull no data. Do not run in Claude Code plan mode — the session's output is a file inside the repo. Then set the Phase 2 row in the Section 9 table to `planned` and link the file.

## 2026-08-05

**Phase:** Project-wide — documentation migration

Brought the repo into compliance with the `hda-*` plugin suite, which did not exist when the repo was built in July. **Structure:** created `.planning/` holding PLAN.md, `phases/`, and this log; deleted root `PLAN.md`, `EXECUTION-PLAN.md`, and `plans/`. Root now carries README.md and CLAUDE.md only, per `hda-docs`. **PLAN.md** was rebuilt to the report specialization: 12 sections, with the former Section 3 conventions moved wholesale into CLAUDE.md and replaced by a pointer, the former Section 1 "decisions locked" list converted into the append-only Decisions table, the former Section 10 becoming Project Don't, a new Project Verify section derived from the old Task 14 DoD, and a new Section 9 phase table. **Phase decomposition:** the old 14 tasks in phases A through E become 7 phases — 1 setup and baselines (complete, tasks 1 and 2), 2 demand data (task 3), 3 market data (task 4), 4 PUMS engine (task 5), 5 burden and instability data (task 6), 6 regional chapters (tasks 7 through 11), 7 local summaries, tracker, and assembly (tasks 12 through 14). Task numbering survives inside the phase files so commit scopes stay `task-N` and the existing history stays continuous. Decision 9 (every task opens with a plan-mode session) is superseded by the `hda-docs` phase plan gate, and the Decisions table records the supersession rather than deleting the row. **CLAUDE.md** was rewritten to the `hda-docs` base skeleton: it now declares the repo type (Report) as the first line, which is what `hda-git:commit` checks first; the hand-written commit style guide was deleted in favor of a short block naming the seven permitted types plus a pointer to `hda-git:commit`; and the domain conventions that were split between CLAUDE.md and PLAN.md Section 3 now live only in CLAUDE.md. The end-of-session checklist adopts the five-step close, with steps 1 and 2 automated by `hda-docs:wrap`. **README.md** was repointed at `.planning/`, given the status line a report repo carries in place of a changelog, and stripped of its horizontal rules. **Mechanical sweeps disclosed:** hard-wrapped paragraphs rejoined to one line each, `§N` converted to "Section N", horizontal rules removed, headings converted to sentence case, contractions removed from reference prose, across README.md, CLAUDE.md, and PLAN.md. This inflates the diff on nearly every line of those three files. Log entries below were exempt — `hda-docs` treats a session log as a carve-out that is never restyled or revised, so they keep their `§` notation and their original wrapping. **GitHub:** created the five house labels (`task`, `bug`, `docs`, `question`, `blocked`) and opened one `task` Issue per remaining phase plus a `docs` Issue for this migration. **Also fixed:** the stale "verbatim fhfh donor" banner in `.claude/skills/new-chapter/references/exemplar-chapter.qmd`, an outstanding follow-up since 2026-07-15. **Verified by running:** Jonathan ran `quarto render index.qmd`, which completed clean and wrote `docs/index.html` — `.planning/` is invisible to Quarto, as the dot prefix intends. The render artifact was discarded rather than committed, since the only change was the re-stamped `date: today` field. No data was pulled, so there are no 2022 percentage changes. **renv re-snapshotted:** the render surfaced a standing out-of-sync warning that predates this work. `renv::status()` showed eight R recommended packages (class, cluster, MASS, Matrix, nlme, nnet, spatial, survival) newer in the library than in the lockfile, because the July snapshot ran under R 4.6.0 and both machines now run 4.6.1. Re-snapshotted with `type = "all"` to match the original Task 1 snapshot — a default dependency-scan snapshot would have dropped every package no current script references yet, which is most of the lockfile while `r/baseline.R` remains the only script. Chose snapshot over restore because restore would try to downgrade base-recommended packages living in the system library. The R pin moves 4.6.0 to 4.6.1 and `renv::status()` no longer reports a lockfile-versus-library difference; it still lists installed-but-unused packages, which is expected until the Phase 2 through 5 scripts land. **Open items carried forward from the entries below, now surfaced rather than buried in prose:** (1) `FRED_API_KEY` is missing from both `.Renviron` locations on the laptop and blocks every FRED pull in Phase 3 — Jonathan to add the value; (2) the Section 5 inventory row for `r/acs_income.R` lists S1701 where a tester expected B25074, unresolved since 2026-07-15 and due for a decision when Phase 2 is planned; (3) the 2022 report's "region" means the 4 primary localities, not the 8-locality PlanRVA study area, so every `geography == "region"` row in `baseline_2022` is scope-shifted — owed to the Phase 7 data-notes appendix; (4) `get_pums()` acceptance of the 2022-vintage PUMA codes for the 2020-2024 5-year PUMS is unverified and must be checked in Phase 4.

## 2026-07-17 (same day, follow-up)

**Phase:** 1 — Setup and baselines

**Data-plan Google Doc created** (Sonnet 5). Jonathan connected the Google Workspace MCP mid-session; re-ran the export via `import_to_google_doc` (markdown content passed inline — the MCP server runs in a separate environment from the local filesystem, so `file_path`/`file://` to the scratchpad file was not resolvable; `content` worked directly). Doc: **[PHA 2026 Update — Streamlined Data Plan](https://docs.google.com/document/d/19io3xXoNZJyspWaCG1KtW_5tKw62p_S6PnSRBuxg8QU/edit?usp=drivesdk)**, created in Jonathan's Drive root. Verified via `get_doc_as_markdown` — headings, all 4 tables, and bold formatting rendered correctly. Per the task constraint, **no sharing permissions were set**; Jonathan shares with PHA. Task 2 DoD is now fully met — **Task 2 complete**.

## 2026-07-17

**Phase:** 1 — Setup and baselines

**Task 2 Session 2B (baselines + data plan) — baseline done, data-plan doc blocked** (Sonnet 5). **Baseline transcription:** wrote `r/baseline.R` — a hand-transcribed data drop (not computed/fetched, per decision 4) reading the rendered `<hda>\rrh-framework\docs\` site. **Extraction method deviation from the plan:** the browser MCP could not open `file://` paths on the `R:\` mapped drive (repeated `navigate`/`preview_start` failures, incl. a 300s timeout) — fell back to the plan's documented fallback ("Read the HTML directly"), but the rendered pages are 3.9–13.2 MB each (Quarto HTML with embedded JS/CSS), too large to `Read` raw without blowing context. Wrote a small Python `html.parser` extractor (scratchpad-only, not committed) that pulls just `<main id="quarto-document-content">` text, stripping `script`/`style`/`nav`/sidebar; verified it against a spot-checked page before running it across all 19 rendered pages (part-1-1..4, part-2-1..4, part-3-1..2, part-4-1..9). Confirmed the rrh charts are ggiraph SVGs with **zero embedded `<text>`/`<tspan>` elements** (path-only rendering) — headline numbers exist only in the prose/table text, which the extractor captures; no chart-data extraction was needed or possible. **Frame:** 142 rows, 6 sections (`demand`/`ownership`/`rental`/`gaps`/`burden`/`local`), 16 distinct geographies (rrh's 4-primary-locality `region` + all 9 individual localities incl. Ashland + `richmond_msa` + `greater_richmond_coc` + 4 CoStar submarket splits) — exceeds the "2–3 signature numbers per locality" floor (`local` section alone has 3–6 rows per locality). `data/baseline_2022.rds` + `data-out/baseline_2022.csv` written; `Rscript r/baseline.R` ran clean under R 4.6.1; structural `stopifnot()` passed (all 6 sections present, no NA in `value`/`metric`/`geography`, all `pha`/`secondary`/`ashland` geographies covered under `section == "local"`, `unit` values constrained). **Important scope note for `data-notes.qmd` (Task 14):** rrh's own report text defines "the region" as the **4 primary localities only** (Chesterfield/Hanover/Henrico/Richmond) — it predates this update's 8-locality + Ashland PlanRVA study area. All `geography == "region"` rows in `baseline_2022` mean rrh's narrower scope, not the `rr`(8) set; this is a vintage-comparison caveat that belongs in the Task 14 appendix. **Data surprises (2 flagged, not resolved — logged per row `note` + here):** (1) rrh's own rental-gap figure (part-3-1, §9.1) states both the 2015 **and** 2019 80%-AMI regional gap as 38,778, despite also stating a +1,220 increase between them — an internal inconsistency in the 2022 source itself, transcribed verbatim on both rows with a flag; (2) Richmond's NOAH count differs between the regional NOAH page (part-2-4: 11,253 units, 45% of all regional NOAH) and the Richmond local-summary page (part-4-1: 128 properties, "more than 9,100" apartments, ~25% of city multifamily) — likely different criteria/vintages between the two rrh pages, both transcribed with cross-referencing notes. **Spot-check (10+ required):** re-verified 12 transcribed values directly against the raw rendered HTML (not just the extracted text) via `grep` — all 12 exact matches: Richmond median owner income $79,858 (part-4-1) · Richmond median renter income $36,249 (part-4-1) · Hanover median home price $423,250, Sept 2022 (part-2-1) · Chesterfield SF permits 2,202 (part-2-1) · regional NOAH "194 properties" (part-2-4) · PIT count "497 in 2019 to 834 in 2021" (part-3-2) · New Kent population 22,945 (part-4-8) · Powhatan median home price $522,181 (part-4-9) · Goochland avg asking rent $2,208 (part-4-7) · Charles City population 6,773 (part-4-6) · Ashland median home price $305,000 (part-4-5) · Henrico rental gap 12,184 (part-4-3). **Data-plan Google Doc — blocked, not built:** no Google Workspace MCP connector is available in this session (`list_connectors`/`search_mcp_registry` both returned empty for google/docs/drive/workspace keywords — no `start_google_auth` tool surfaced). Per the task's own instruction this does **not** gate Phase B. The streamlined data-plan content (light intro + Tables A–D from §5, in PHA-facing plain language) is fully drafted at `scratchpad/pha-streamlined-data-plan.md` (this session's scratchpad — not committed), ready to paste into a Doc or export once Jonathan connects Google Workspace (Drive/Docs) to Claude. **Open for Jonathan:** connect the Google Workspace MCP connector, then either paste the drafted markdown into a Doc directly or ask a future session to run the export + share with PHA. **Commit:** `data(task-2-s2b): transcribe 2022 baseline frame + export data plan` (baseline only — doc export deferred).

## 2026-07-16

**Phase:** 1 — Setup and baselines

**Laptop onboarding + doc portability** (Opus 4.8). Cloned the repo to a second machine (`C:\repos\hda\pha-update-2026`, user **jonat**) and audited every desktop hard-code. **Env audit (this laptop):** R **4.6.1** installed at `C:\Program Files\R\R-4.6.1` (plus 4.4.2/4.5.0) — **no 4.6.0, none at `C:\R\`**; R and Quarto both installed but **not on PATH** (Quarto at `C:\Program Files\Quarto\bin`); **renv library absent** (fresh clone → `renv::restore()` needed); `CENSUS_API_KEY` + `GITHUB_PAT` present in both `~/.Renviron` and `~/Documents/.Renviron`, but **`FRED_API_KEY` is MISSING from both** (blocks FRED pulls — Jonathan to add the value); sibling repos (fhfh, rrh-framework, faar, hdatools) present at `C:\repos\hda\`, **no `R:\` drive**. No committed `r/*.R` yet, so all JTK/`C:\R`/`R:\` hard-codes lived only in docs + the `new-data-script` skill. **Decisions:** run under installed **R 4.6.1** (same `R-4.6` renv library, patch-compatible; renv.lock R pin left at 4.6.0 — a restore version warning is expected/harmless); rewrote references **machine-portable** rather than swapping one hard-code for another. **Edits:** `.Renviron` fallback → user-derived `file.path(Sys.getenv("USERPROFILE"), "Documents", ".Renviron")` (CLAUDE.md, PLAN.md, and the skill's `exemplar-script.R`/`conventions.md`/`SKILL.md` so newly-scaffolded scripts don't re-introduce `JTK`); PATH guidance → dual-machine examples + **added Quarto** to the prepend (CLAUDE.md, README.md, PLAN.md); "R 4.6.0 render target" → "R **4.6.x**, install path machine-specific"; introduced an `<hda>` convention (= local parent of the sibling repos) and converted the live `R:\hda\…` reference/provenance pointers in PLAN.md §2/§5/§9 and EXECUTION-PLAN.md §1. **Left verbatim (historical records):** the dated §11 entries above (incl. the desktop's `C:\R\R-4.6.0`) and `plans/task-2-skills-baselines.md`. **Not run this session (plan/doc only):** `renv::restore()`, key-visibility check, and a smoke render — handed to Jonathan (see the onboarding checklist). **No data pulled → no 2022 % changes.** Commit: `docs: make setup docs machine-portable`.

## 2026-07-15 (fourth session)

**Phase:** 1 — Setup and baselines

**Task 2 `/new-data-script` revision** (Opus 4.8; unplanned mid-task session, skill-creator-driven upgrade of the 2A build). Re-evaluated `/new-data-script` via the official `skill-creator` skill (its structural + triggering lens, not a smoke test) and applied three changes. **(1) purrr 1.2.2 migration:** renv pins **purrr 1.2.2** (renv.lock:3817) where `map_dfr()`/`map_dfc()` are *superseded* by `map()`/`imap()` + `list_rbind()`/`list_cbind()` — migrated the SKILL.md skeleton comment + `conventions.md` year-trend pattern off `map_dfr()`, and propagated the corrected idiom to **CLAUDE.md** (Script anatomy line) and **PLAN.md §3** (R-standards) so the first-class docs match the skill. Not a hard failure (superseded ≠ defunct), but off-standard for the project's "current best-practice" ethos. **(2) Exemplar is now canonical (decision changed from 2A):** rewrote `references/exemplar-script.R` from a verbatim fhfh donor (with an "adapt, don't copy" banner + `case_match` throughout + no `export_csv`) into a fully best-practice reference that *is* what the skill should emit — an `acs_demographics` pull using an `imap()`+`list_rbind()` `pull_acs()` helper (factoring fhfh's 6× repeated county+place+state bind), `case_when()` recodes with `%in%` ranges, `source("_common.R")`, `write_rds`+`export_csv` pairing, 0–100 CV, `.Renviron` fallback, and structural-only `stopifnot` + 2022-logged-not-failed (behind a `file.exists` guard). Dropped the now-obsolete `case_match` caveat in `conventions.md`; aligned the SKILL.md skeleton so the exemplar reads as a filled-in instance (added the `pull_acs()` helper to the skeleton; fixed `GEOID`→`geoid` post-`clean_names()`). **What skill-creator surfaced (ranked):** the exemplar *contradicting* the stated standard (case_match vs the case_when rule; missing export_csv/2022-logging) was the highest-value defect — a fresh run copying the reference would emit off-standard code; the `map_dfr` staleness and skeleton↔exemplar drift followed. Description/triggering judged already solid (pushy, concrete trigger phrases) — no change; a `run_loop.py` triggering-optimization pass remains available but deferred (long job, low marginal value). **(3) Verified:** re-swept the skill dir (no `map_dfr`/`case_match` usages remain — only "avoid this" guidance), and ran a fresh-context subagent that invoked the revised skill to scaffold `r/acs_income.R` (B19013/B19001/B25074) to **scratchpad** (repo untouched). Output was a faithful filled-in instance of the new exemplar: `pull_acs()` via `imap`+`list_rbind`, zero `map_dfr`/`case_match`, `load_variables()`+`separate_wider_delim` label joins, `write_rds`+`export_csv`, structural `stopifnot` with same-vintage benchmark left commented, 2022 logged-not-failed. The tester also flagged a real **PLAN.md §5 inventory** discrepancy (row for `acs_income.R` lists S1701, not the B25074 I named it in the test prompt) — noted, not resolved here. **(4) dplyr 1.2.0 compliance (follow-up ask):** checked the skill against the dplyr **1.2.0** release notes. Verdict: *compliant* (no deprecated calls — `.by`/`reframe()` now stable, `if_else()` CV pattern unaffected) but *not taking advantage*, on a **factual error**: PLAN.md claimed "`recode_values()` is unavailable" in the pinned dplyr, so the project standardized on `case_when()`. Verified against the installed library (`renv/library/.../dplyr` — **DESCRIPTION 1.2.1**; NAMESPACE **exports `recode_values`, `replace_values`, `replace_when`, `when_any`, `when_all`, `filter_out`**): `recode_values()` *is* available. It's the dplyr 1.2.0 value-mapping function and the named replacement for the soft-deprecated `case_match()` — exactly the value→label recodes the skill performs. Per Jonathan's call, **adopted `recode_values()`** for value maps (exemplar age-band + race recodes; SKILL.md skeleton; conventions.md) with `case_when()` reserved for conditional logic; corrected the false "unavailable" premise in **PLAN.md §3 (line 106) + §8 gotchas (line 156)** and **CLAUDE.md**; noted `replace_values()`/`when_any()`/`when_all()`/`filter_out()` as available. `recode_values()` interface (`old ~ new`, `c()` grouping, `default =`/`unmatched =`) confirmed against the dplyr 1.2.0 source — **not** executed (Windows R rule; runtime behavior unverified). **No 2022 % changes** this session (no live data pulled). **Follow-up:** `/new-chapter` carries the same 2A "verbatim donor (adapt, don't copy)" exemplar banner (`references/exemplar-chapter.qmd:2`) — apply the same exemplar-is-canonical treatment in a later session (no `map_dfr` in it, so no purrr migration needed there). Commit: `infra(task-2-s2a)`.

## 2026-07-15 (third session)

**Phase:** 1 — Setup and baselines

**Task 2 Session 2A (Skills) complete** (Opus 4.8). Built two in-repo, project-level skills at `.claude/skills/{new-data-script,new-chapter}/`, each = `SKILL.md` (lean body, YAML `name`+`description` frontmatter for triggering) + `references/` (a verbatim real fhfh exemplar + a `conventions.md` digest of PLAN.md §3/§6/§7). **Universal design:** both are project-agnostic and carry a **project-config block** the skill populates by reading the target project's `CLAUDE.md` + `PLAN.md` + `_common.R` at invocation — no pha paths/geographies hardcoded, so the later user-level lift is clean. `/new-data-script` emits the header→numbered-pulls→`write_rds`+`export_csv`→validation-block anatomy (0–100 CV, `case_when`, `.Renviron` fallback, structural-only `stopifnot` + 2022-logged-not-failed); exemplar = fhfh `acs_demographics.R`. `/new-chapter` emits `#sec-slug`→setup(`read_rds` only)→takeaway-H2s→figure/table+bullet blocks→per-section "Since 2022" callout; exemplar = fhfh `demographics.qmd`. **Git-tracking confirmed:** Task 1's `.gitignore` never touched `.claude/` (`git check-ignore` exit 1); no un-ignore rule needed. **Smoke-tested** both in throwaway subagent contexts (scaffolded `r/acs_tenure.R` and `ownership.qmd` to scratchpad, repo untouched) — both emitted correctly pha-shaped scaffolds (right `_common.R` sourcing, pha geo constants, `theme_pha`/`pha_pal`, `flag_reliability`, `case_when`, fig-alt, no `@`-refs in captions). **Iterated once** on tester findings: added "adapt, don't copy" banners to both exemplars (they're real fhfh donors — `case_match`/`theme_hda`/`hda_pal`/Fauquier geo are copy-traps), plus guidance for the no-same-vintage-benchmark case, the data-script/chapter reliability boundary, derived-data caption helpers, and change-callout placement (not every H2 has a 2022 analogue). Docs: updated CLAUDE.md ("Skills carry the boilerplate" note + repo map) and README ("What's in here" table). **Skills are project-level for now** (in-repo → a fresh clone has them); **elevation path:** once they prove out across the data (Tasks 3–6) and chapter (7–11) work, lift to `~/.claude/skills/` with any evidence-based revisions. Commit: `infra(task-2-s2a)`. **Open for Task 2:** Session 2B (Sonnet) — transcribe `baseline_2022` + export the §5 data plan to a Google Doc.

## 2026-07-15 (second session)

**Phase:** 1 — Setup and baselines

**Task 2 planned** (Opus 4.8, plan-mode-only per decision 9). Wrote `plans/task-2-skills-baselines.md` (creates the `plans/` dir). Structure: **2 execution sessions** — **2A** skills (Opus): build `/new-data-script` + `/new-chapter` as universal but **project-level** skills (in-repo `.claude/skills/`, elevated to user-level once proven on Tasks 3–11) parameterized on a project-config block read from the target project's PLAN/CLAUDE, exemplars sourced from fhfh `r/`+`.qmd` (pha-update has no finished scripts/chapters yet), **lightweight** build depth (draft + smoke-test, no skill-creator eval loop); **2B** baselines + data plan (Sonnet): `r/baseline.R` transcribes a **comprehensive** 2022 headline frame (all 5 regional sections + 2–3 signature figures per locality) from the rendered rrh site → `data/baseline_2022.rds` + `data-out/baseline_2022.csv` (structural stopifnot only; spot-check ≥10), then export §5 to a Google Doc (Claude creates, **Jonathan shares** — setting permissions is a prohibited action for Claude). Confirmed rrh topic→page map (part-1-* demand, part-2-* ownership/rental, part-3-* gaps/burden, part-4-1..9 local). No skills/baseline/doc built this session (plan-mode-only). **Open for Task 2 execution:** run 2A then 2B per the plan's starter prompts.

## 2026-07-15 (first session, part 2)

**Phase:** 1 — Setup and baselines

**Task 1 Session 2 (docs + renv) complete** (Opus 4.8) — this finishes Task 1. Wrote **PLAN.md** (this file — fhfh 11-section structure, PHA content: §5 dataset inventory / streamlined data plan, §9 task blocks with DoD/Don't, §11 log), **CLAUDE.md** (fhfh conventions + this project's token-efficiency rules), **README.md** (plain-language quick-start + a teammate renv guide), and **`.renvignore`** (excludes data/docs/_freeze/archive from dependency scan). Committed `8269d55`, pushed. **renv (Jonathan-run):** `renv::init(bare=TRUE)` → installed the 14-package toolkit + `hdadvisors/hdatools` → `renv::snapshot(type="all")`. **Locked versions of note:** **dplyr 1.2.1** (→ `case_when()`, not `recode_values()`), **hdatools 0.1.7**, **ggplot2 4.0.3** (the S7 `strip.text` gotcha applies), tidycensus 1.8.1, tigris 2.2.1, sf 1.1-1, srvyr 1.3.1, renv 1.2.3. **`ggiraph 0.9.6` pulled in transitively as an hdatools dependency** — it's in the lockfile but the "static-only" decision governs *our* code, so no action; do not use it. **R-version correction (important):** `renv::init()` ran under **R 4.6.0** (found at `C:\R\R-4.6.0`, outside Program Files) — so renv.lock is pinned to **R 4.6.0** and the library is built for **R-4.6**. The Session 1 render used R 4.5.1 + the system library (pre-renv); now that `.Rprofile` activates the version-specific renv library, **renders must use R 4.6.0** or the packages won't load. Updated the run commands in PLAN/CLAUDE/README from R-4.5.1 → `C:\R\R-4.6.0`. **Re-verified `quarto render` clean under R 4.6.0 + renv** — 7 pages, no errors. renv files (`renv.lock`, `renv/activate.R` + `renv/settings.json`, `.Rprofile`) + the RStudio `.Rproj` committed this session. **Deviations:** (1) render target R version corrected 4.5.1 → 4.6.0 post-renv (see above); (2) no `plans/` dir yet (Git can't track an empty dir — Task 2's planning session creates it). **Task 1 DoD met:** skeleton renders clean under the locked env, public repo pushed + Pages enabled, all 6 Session-1 + 4 Session-2 items complete. **Open for Task 2:** its Opus planning session; build the two skills; transcribe `baseline_2022` from the rendered rrh site; export the §5 data plan to a Google Doc. **Verify at Task 5:** that `get_pums()` accepts the 2022-vintage PUMA codes for the 2020-2024 5-year PUMS.

## 2026-07-15 (first session, part 1)

**Phase:** 1 — Setup and baselines

**Task 1 Session 1 (Scaffold) complete** (Opus 4.8). Archived all SOH assets → `archive/soh-2026/` (3 scripts, `img/` 7 PNGs, the pptx, and all 8 stale `data/` files). `git init` on `main`; created **public** repo `hdadvisors/pha-update-2026`; enabled GitHub Pages from `main`/`docs` (`https://hdadvisors.github.io/pha-update-2026/`). Wrote `.gitignore` (commits `r/`/`docs/`/`_freeze/` + public-source `data-out/`; ignores `data/` except `data/raw/README.md`, plus `data-out/mls_*`/`costar_*`) — verified with `git check-ignore`. Built the Quarto book skeleton: `index.qmd` + 5 section stubs (`demand`/`ownership`/`rental`/`gaps`/`burden`) + `data-notes.qmd` appendix; `_quarto.yml` (rrh book config + fhfh `execute-dir: project`/`freeze: auto`; **noindex** robots meta + **Hypothesis** comments); copied `rrh-framework.scss` + `img/pha_logo.jpg`. `_common.R`: `pha_pal` (SOH hexes) + `cb_pal`; geography constants `rr`(8)/`pha`(4)/`secondary`(4)/`ashland`/`virginia`; PUMS `puma_core3`/`puma_region`/`puma_locality`; caption helpers (`acs`/`pums`/`chas`/`dec`/`pep`/`wc`/`bps`/`mls`/`costar`/`fmr`/`ami`/`nhpd`/`posh`/`lihtc`/`oews`/`pit`/`cpi`/`pmms`); `flag_reliability()` (0–100 CV, 15/30); `fct_wrap()`; `export_csv()`. `data/raw/README.md` = MLS/CoStar export spec (fields grounded in the archived exports' actual columns + fhfh `mls.R`/`costar.R` expectations). **Geography resolution (tigris, not memory — the note in `_common.R`):** Ashland town place FIPS = **5103368** (my memory guess 5102270 was wrong — validates the "resolve, don't recall" rule); 2020 PUMAs resolved for `year=2022` — core-3 = 04101/04102/04103 (Chesterfield), 08701/08702 (Henrico), 76001/76002 (Richmond); Hanover confirmed **not isolable** (split across 08501 + 14501, each multi-county) → core-3 is the right regional default. **Render:** `quarto render` clean — 7 HTML pages in `docs/`, noindex meta on all 7, Hypothesis present, logo + `_freeze/` produced (initial S1 render under R 4.5.1 + system library; re-verified clean under the locked R 4.6.0 + renv env in S2). **Deviations from the EXECUTION-PLAN Task 1 list:** (1) `_quarto.yml` is **HTML-only** — `downloads: pdf` + scrreprt config deferred to Task 14 so the skeleton renders clean without a LaTeX/typst toolchain (fhfh deferred its PDF route the same way); (2) `.Rprofile`/`.renvignore` **not** created in Session 1 — an `.Rprofile` sourcing the not-yet-existing `renv/activate.R` would break the Session 1 render; both come with `renv::init()` in Session 2; (3) chapter files named by theme (`demand`/`ownership`/…) rather than rrh's `part-N-M` scheme, matching the reduced 5-section preliminary structure. **First commit** `b772e1c`, pushed. **Open for Session 2:** PLAN.md (this file — done), CLAUDE.md, README.md, renv config + Jonathan runs init/snapshot.
